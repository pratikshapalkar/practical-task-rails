<%= form_with(model: comment_params(tagable, Tag.new), local: true) do |form|%>
<div class="field">
<%= form.label :content %><br/>
<%= form.text_area :content %>
</div>
<%= form.submit class: "btn btn-primary"%>
<% end %>
